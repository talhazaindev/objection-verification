services:
  objection:
    build: .
    image: objection-prototype
    container_name: objection-prototype
    ports:
      - "8080:8080"
    env_file:
      - backend/.env
    restart: unless-stopped
